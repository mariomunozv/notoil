
<br>
<div class="panel panel-default">
    <div class="panel-heading">
        <div class="panel-title">Editar Perfil</div class="panel-title">
    </div>
    <div class="panel-body">
        <%= form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => { :method => :put }) do |f| %>
        
        <div class="row">
            <div class="col-sm-4">
                <div class="form-group">
                    <div>Foto de perfil</div>

                </div>
            </div>
            <div class="col-sm-8">
                <% if resource.errors.messages.count!=0 %>
                    <div id="error_explanation">
                        <h4>Se detectaron los siguientes errores:</h4>
                        <ul>
                            <% resource.errors.messages.each do |msg| %>
                            
                            <li><%= msg[1][0] %></li>
                            <% end %>
                        </ul>
                    </div>
                <% end %>
                <div class="form-group">
                    <div><%= f.label :email %><br />
                    <%= f.email_field :email, :autofocus => true, :class => "form-control input-sm" %></div>
                </div>
                <div class="form-group">
                    <div><%= f.label :name %><br />
                    <%= f.text_field :name, :class => "form-control input-sm" %></div>
                </div>
                <div class="form-group">
                    <div><%= f.label :lastname %><br />
                    <%= f.text_field :lastname, :class => "form-control input-sm" %></div>
                </div>
                <div class="form-group">
                    <div><%= f.label :phone %><br />
                    <%= f.text_field :phone, :class => "form-control input-sm" %></div>
                </div>
                <div class="form-group">
                    <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
                    <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
                    <% end %>
                </div>
                <div class="form-group">
                  <div><%= f.label :password %> <i>(Dejar en blanco si no quieres cambiarla)</i><br />
                  <%= f.password_field :password, :autocomplete => "off", :class => "form-control input-sm" %></div>
                </div>
                <div class="form-group">
                    <div><%= f.label :password_confirmation %><br />
                    <%= f.password_field :password_confirmation, :class => "form-control input-sm" %></div>
                </div>
                <div class="form-group">
                    <div>
                        <%= f.label :current_password %> <i>(Es necesario escribir la contraseña actual para cambiar a una nueva)</i><br />
                        <%= f.password_field :current_password, :class => "form-control input-sm" %>
                    </div>
                </div>
                <div><%= f.submit "Actualizar", :class => "btn btn-success float-right" %></div>
            </div>
        </div>

        <% end %>
    </div>
</div>
<div class="panel panel-default">
    <div class="panel-heading">
        <div class="panel-title">Cancelar mi cuenta</div >
    </div>
    <div class="panel-body">

        <p>Quieres cancelar tu cuenta? <%= button_to "Eliminar", registration_path(resource_name), :data => { :confirm => "Are you sure?" }, :method => :delete, :class => "btn btn-danger" %></p>

        <%= link_to "Volver", :back %>
    </div>
</div>
